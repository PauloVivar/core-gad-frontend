services: 
  core-gad-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args: 
        VITE_API_BASE_URL: http://172.26.11.49:8001
        VITE_API_PAYMENT_BASE_URL: http://172.26.11.49:8080
    ports:
      - 80:80
    volumes:
      - ./src:/app/src
